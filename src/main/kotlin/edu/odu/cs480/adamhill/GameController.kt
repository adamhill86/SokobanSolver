package edu.odu.cs480.adamhill

class GameController(initialState: Node<State>, val obstacles: Barriers,
                     private val gameRows: Int, private val gameCols: Int) {
    /*
    val graph = Graph(initialState) // create a graph with the initial state as the root

    /**
     * Calculate all possible robot moves
     */
    fun calculateMoves() {
        // TODO: Move calculateMoves function into the bfs and dfs functions. I can't think of a better way to do that right now.
    }

    /**
     * Calculate whether a robot move is legal
     * @param nextPosition The position of the potential move
     * @return True if the move is legal, false otherwise
     */
    private fun isLegalMove(nextPosition: Point): Boolean {
        // for now, only check to see if the position matches an obstacle's position
        if (obstacles.contains(nextPosition)) return false

        return true
    }*/
}